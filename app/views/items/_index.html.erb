<%= link_to item_path(item.id) do %>
	<%= attachment_image_tag item, :item_image, :fill, 300, 300 %>
<% end %>
<br>

<strong><%= item.title %></strong>
<br>

<strong><%= item.price %>円</strong>
<br>

<strong>サイズ : </strong>
<%= item.size %>
<br>

<%= link_to "削除", item_path(item.id), data: { confirm: "削除してもよろしいですか？" }, method: :delete %>
<br>