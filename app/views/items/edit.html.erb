<strong class="text-center">出品物編集ページ</strong>
<%= form_with(model: @item, local: true) do |f| %>
	<%= render 'items/form', item: @item, f: f %>
	<%= f.submit "更新する" %>
<% end %>
<br>

<%= link_to "戻る", item_path(@item.id) %>